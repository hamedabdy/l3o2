<html lang="en">
<head>
	<meta charset="utf-8" />
	<title>Admin Dashbord</title>
	<link rel="stylesheet" href="//code.jquery.com/ui/1.10.3/themes/smoothness/jquery-ui.css" />
	<script src="//code.jquery.com/jquery-1.9.1.js"></script>
	<script src="//code.jquery.com/ui/1.10.3/jquery-ui.js"></script>
	<link rel="stylesheet" href="//jqueryui.com/resources/demos/style.css" />
    <link href="../css/bootstrap.css" rel="stylesheet">
    <link href="../css/flat-ui.css" rel="stylesheet">
</head>
<body>
<div class="center-position">
<% if (!user) { %>
	<h4>If you are a admin please log in</h4>
	<p style=" font-size: 12px;">
	<a href="/" class="btn btn-large btn-block">Home</a> |
	<a href="/login" class="btn btn-large btn-block btn-info">Log In</a>
	</p>
<% } else { %>
	<h4>Hi!</h4>
	<p style=" font-size: 12px;">
	<a href="/" class="btn btn-large btn-block">Home</a> |
	<a href="/account" class="btn btn-large btn-block btn-primary" style="text-transform: capitalize;"><%= user.username %></a> |
	<a href="/importAds" class="btn btn-large btn-block btn-info">Import Ads</a> |
	<a href="#" id="opener-as" class="btn btn-large btn-block btn-danger">Update Database From Last.fm</a> |
	<a href="#" id="opener-sk" class="btn btn-large btn-block btn-danger">Update Database From SongKick</a> |
	<a href="/logout" class="btn btn-large btn-block btn-inverse">Log Out</a>
	</p>
	<% if (message) { %>
	<p class="messege"><%= message %></p>
	<% } %>
<% } %>
</div>
	<div id="dialog-confirm-as" style="font-size: small;" title="Update Database from Last.fm?">
	<p><span class="ui-icon ui-icon-alert" style="float: left; margin: 0 7px 20px 0;"></span>Are you sure?</p>
</div>
</div>
	<div id="dialog-confirm-sk" style="font-size: small;" title="Update Database from SongKick?">
	<p><span class="ui-icon ui-icon-alert" style="float: left; margin: 0 7px 20px 0;"></span>Are you sure?</p>
</div>
	<script>
	$(function() {
		$( "#dialog-confirm-as" ).dialog({
			autoOpen: false,
			resizable: false,
			height:140,
			modal: true,
			buttons: {
				"Do it!": function() {
					window.location= "/maj-as" ;
				},
				Cancel: function() {
					$( this ).dialog( "close" );
				}
			}
		});
	});
	$("#opener-as").click(function(){
		$("#dialog-confirm-as").dialog("open");
	});

	$(function() {
		$( "#dialog-confirm-sk" ).dialog({
			autoOpen: false,
			resizable: false,
			height:140,
			modal: true,
			buttons: {
				"Do it!": function() {
					window.location= "/maj-sk" ;
				},
				Cancel: function() {
					$( this ).dialog( "close" );
				}
			}
		});
	});
	$("#opener-sk").click(function(){
		$("#dialog-confirm-sk").dialog("open");
	});
	</script>
</body>
</html>